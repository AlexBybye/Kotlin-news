# 新闻 APP 课程大作业计划概览

## 1. 项目目标

本项目拟设计并实现一个用于课程大作业展示的 Android 新闻 APP，主题定位为"校园热点 + 综合新闻"。项目重点不是只完成基础页面，而是尽量覆盖 Android 开发中的主流技术与典型组件，形成一个具备展示价值、结构清晰、可分阶段推进的完整应用。

核心目标如下：

- 完成一个可运行、可演示、可扩展的新闻 APP。
- 覆盖 Android 常见开发能力，包括多 Fragment、Navigation、Room、Retrofit/OkHttp、RecyclerView、ViewModel、数据持久化、异步加载等。
- 形成清晰的阶段性目标，便于后续逐目标制定开发计划与验收标准。

非目标如下：

- 不以商业级推荐算法或完整用户系统为本期重点。
- 不强求服务端自建，优先采用公开新闻接口或本地 Mock 数据完成演示。

## 2. 产品定位

### 2.1 使用场景

- 用户浏览推荐新闻与分类新闻。
- 用户查看新闻详情、收藏文章、搜索感兴趣内容。
- 用户在弱网或无网场景下查看缓存内容。
- 用户根据个人偏好调整主题、字号与刷新策略。

### 2.2 用户价值

- 快速获取热点新闻信息。
- 支持收藏和历史记录，提升内容回访效率。
- 在课程答辩中清晰展示 Android 应用的分层设计与组件协作。

## 3. UI 概览

### 3.1 整体导航结构

采用 `Single Activity + BottomNavigation + Multi Fragment` 的结构：

- 首页 `HomeFragment`
- 发现 `DiscoverFragment`
- 收藏 `FavoriteFragment`
- 我的 `ProfileFragment`

同时补充二级页面：

- 新闻详情 `NewsDetailFragment`
- 搜索页 `SearchFragment`
- 设置页 `SettingsFragment`

### 3.2 页面草图说明

#### 首页

- 顶部：应用标题 / 搜索入口
- 中部：分类 Tab（推荐、科技、体育、校园、国际）
- 内容区：Banner + 新闻列表
- 交互：下拉刷新、点击进入详情、列表分页加载

#### 发现页

- 展示热门话题、专题聚合、热搜关键词
- 支持按来源或主题浏览内容

#### 收藏页

- 收藏文章列表
- 最近浏览记录
- 离线内容入口

#### 我的页

- 用户信息展示区（可为静态占位）
- 阅读统计
- 设置入口
- 关于页面入口

#### 详情页

- 标题、来源、时间、封面图、正文
- 收藏按钮
- 分享按钮
- 相关推荐列表

#### 搜索页

- 搜索输入框
- 热门关键词
- 搜索历史
- 搜索结果列表

#### 设置页

- 深色模式
- 字号大小
- 是否仅 Wi-Fi 加载大图
- 刷新频率
- 清除缓存

## 4. 可用功能范围

### 4.1 核心功能

- 新闻分类浏览
- 新闻详情查看
- 搜索新闻
- 收藏新闻
- 浏览历史记录
- 本地缓存与离线展示
- 个性化设置

### 4.2 加分功能

- 热点自动刷新
- 通知提醒
- 分页加载
- 加载态、空态、错误态统一处理
- 原文跳转或 WebView 扩展

## 5. 技术覆盖规划

| 技术点 | 规划用途 |
|------|------|
| Fragment | 首页、发现、收藏、我的、详情、搜索、设置等页面拆分 |
| Navigation Component | 底部导航与页面跳转管理 |
| ViewModel | 管理页面状态与数据加载逻辑 |
| LiveData / StateFlow | 驱动 UI 状态更新 |
| RecyclerView | 新闻列表、搜索结果、收藏列表 |
| Retrofit | 新闻接口请求 |
| OkHttp | 拦截器、日志、超时、统一请求处理 |
| Room | 收藏、历史、缓存、搜索历史存储 |
| Repository | 统一协调本地数据与远程数据 |
| DataStore | 设置项持久化 |
| WorkManager | 定时刷新新闻、推送提醒 |
| Coil / Glide | 图片加载 |
| SwipeRefreshLayout | 刷新交互 |
| Paging 3 | 新闻分页加载 |

## 6. 建议架构

推荐采用 `MVVM + Repository`：

`UI(Fragment)` -> `ViewModel` -> `Repository` -> `RemoteDataSource / LocalDataSource`

分层说明如下：

- `ui`：页面与交互逻辑
- `viewmodel`：页面状态管理
- `data/remote`：网络接口与数据模型
- `data/local`：Room 数据库、DAO、Entity
- `repository`：整合网络与本地数据
- `utils/common`：网络状态、结果封装、工具类

## 7. 阶段目标拆分

### 目标 1：完成项目骨架

范围：

- 搭建 `Single Activity`
- 配置 `BottomNavigation`
- 接入 `Navigation Component`
- 创建 4 个主 Fragment

验收标准：

- 应用可启动
- 底部导航可正常切换
- 页面结构完整

### 目标 2：完成首页新闻列表

范围：

- 接入新闻接口或 Mock 数据
- 展示分类新闻列表
- 支持下拉刷新

验收标准：

- 首页可展示新闻数据
- 分类切换后内容可更新
- 网络异常时可提示失败状态

### 目标 3：完成新闻详情页

范围：

- 列表点击进入详情
- 展示标题、来源、时间、正文、图片
- 支持收藏按钮与相关推荐

验收标准：

- 可从首页进入详情页
- 详情信息展示完整
- 收藏交互可响应

### 目标 4：完成搜索功能

范围：

- 输入关键词搜索新闻
- 展示搜索结果
- 保存搜索历史

验收标准：

- 可输入关键词并返回结果
- 搜索历史可展示且可复用

### 目标 5：完成收藏与历史记录

范围：

- 用 `Room` 保存收藏记录
- 保存用户浏览历史
- 提供对应列表页面

验收标准：

- 收藏后数据可持久化保存
- 重新打开应用后数据仍存在

### 目标 6：完成缓存与离线能力

范围：

- 缓存新闻列表或详情
- 无网时优先读取本地缓存

验收标准：

- 网络不可用时仍可展示部分缓存内容
- 收藏文章支持离线查看

### 目标 7：完成设置中心

范围：

- 深色模式切换
- 字号设置
- 刷新策略与图片加载策略设置

验收标准：

- 设置项可生效
- 设置结果可持久化保存

### 目标 8：完成加分项优化

范围：

- 定时刷新热点
- 通知提醒
- 分页加载
- 加载态优化

验收标准：

- 至少完成 2 个加分能力
- 能在答辩中清晰说明实现方式与组件作用

## 8. 推荐开发顺序

第一阶段：

- 目标 1：项目骨架
- 目标 2：首页新闻列表
- 目标 3：新闻详情页

第二阶段：

- 目标 4：搜索功能
- 目标 5：收藏与历史

第三阶段：

- 目标 6：缓存与离线能力
- 目标 7：设置中心

第四阶段：

- 目标 8：加分项优化
- 整体 UI 与交互完善

## 9. 答辩展示亮点

- 具备完整页面链路：首页 -> 详情 -> 收藏 / 搜索 / 设置
- 覆盖多种 Android 组件与常用架构模式
- 同时包含网络请求、本地数据库、导航、异步加载与持久化能力
- 具备从基础功能到加分功能的递进式开发路线

## 10. 后续计划

后续将按上述 8 个目标逐项细化，每个目标进一步拆分为：

- 页面任务
- 包结构与类清单
- 依赖配置
- 开发步骤
- 验收标准
- 可选优化项
